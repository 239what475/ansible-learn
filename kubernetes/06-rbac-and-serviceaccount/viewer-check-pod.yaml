apiVersion: v1
kind: Pod
metadata:
  name: viewer-check
  namespace: auth-demo
  labels:
    app: viewer-check
spec:
  serviceAccountName: viewer
  containers:
    - name: viewer-check
      image: busybox:1.36
      command: ["sh", "-c", "sleep 3600"]
