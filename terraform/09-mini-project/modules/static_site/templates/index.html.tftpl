<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <title>${site_title}</title>
  </head>
  <body>
    <h1>${site_title}</h1>
    <p>${site_message}</p>
    <ul>
      <li>environment = ${environment_name}</li>
      <li>container_name = ${container_name}</li>
      <li>summary = ${environment_summary}</li>
    </ul>
  </body>
</html>
