# 00: Prestart

这一节只负责指路，帮助你完成正式实操前的本机环境准备。
具体操作写在下面的文档里。

## 文件

- `terraform.md`：安装 `Terraform`、验证命令可用，并先认识后面会高频使用的基础命令

## 推荐顺序

```text
terraform.md -> ../01-init-plan-apply-destroy/
```

## 完成标准

当下面这些都正常时，就可以进入下一章：

- `terraform version` 可以正常输出版本
- `terraform -help` 可以正常输出帮助
- 你知道 `fmt`、`validate`、`init`、`plan`、`apply`、`destroy` 这些基础命令分别是做什么的
